######################Test Pipeline#####################
kind: pipeline
type: docker
name: TEST-Pipeline

trigger:
  branch:
    - release-candidate/*
  event:
    - push
  concurrency:
    limit: 1

steps:
  - name: running pipeline
    image: alpine:3.11.6
    commands:
      - echo uat-1.1
    when:
     event:
       - push
